library(ggplot2)
#source("./R/grid_usa.R")

graph_interp <- function(preds){
  x <- 1
}
  # grid <- grid_usa(200)
  # locs_pred <- grid[, c("x", "y")]
  # ggplot2::ggplot(data = as.data.frame(preds), aes(x = locs_pred[,"x"], y = locs_pred[,"y"],
  #                                         fill = preds)) +
  #   geom_tile() +
  #   scale_fill_viridis_c() +
  #   coord_quickmap() +
  #   theme_minimal() +
  #   theme(legend.position = "bottom") +
  #   labs(title = "Interpolated T_DAILY_AVG values")


